# Glossary

Project vocabulary, acronyms, codenames — each with a one-line decode. Seeded by `scout` 2026-07-02; maintained inline + by `retrospective`.

| Term | Means | Source |
|---|---|---|
| MDLP | Multi-Dimensional Learning Pathway — the algorithm/research project (and the planned `mdlp` Python package) | verified — repo name, RELEASE-PLAN-v2 |
| Turing Agents | The self-evolving Markdown+Bash agent scaffold this repo runs on (v0.2.0); separate version line from the MDLP algorithm | verified — CLAUDE.md, README |
| v0.2 spec | `ALGORITHM-v0.2-pathway-learner.md` — the spec of record, §1–§19, all additions additive | verified |
| `significant()` | The one gate primitive: every commit/rollback/promote/admit tests a delta against its own SE/CI — gates are statistical, never scalar (fixes RC-1) | verified — spec §2 |
| dual posterior | The state model keeps two Beta posteriors per skill cell: mastery (slow decay, `γ_slow`, drives promotion) and drift (fast decay, `γ_fast`, drives rollback only) (fixes RC-5) | verified — spec §3 |
| ĉ / competence point | `αm/(αm+βm)` — point estimate of competence for a skill×difficulty cell, with posterior SE | verified — spec §3 |
| held-out split / secret set | Eval items never shown to the learner; reward is computed ONLY on held-out (principle P1); the generalization gate requires `Δĉ_secret ≥ ρ_gen · Δĉ_public` | verified — spec §4 |
| P1 / P2 | The two design principles: P1 measurement independent of optimization; P2 every `add` has an inverse | verified — spec/HANDOVER |
| RC-1…RC-8 | The 8 root causes from the v0.1 red-team (`ALGORITHM-v0.1-redteam.md`); each must keep a guarding regression test | verified |
| Tutor layer | §13 — generic curriculum strategist with pluggable Teachers; picks actions by expected learning gain | verified — spec §13 |
| A1 | BUILD-SPEC: information-gain selection — Tutor objective `U(a)=(1−w)z(E[Δc])+w·z(EIG)`, closed-form Beta EIG (approved 85/100) | verified — BUILD-SPECS |
| A5 | BUILD-SPEC: warm-start priors from "learners like you" via kNN over the vector store, MMR diversity, influence decays with own evidence (approved 82) | verified — BUILD-SPECS |
| B1 | BUILD-SPEC: misconception clustering → graph-linked remediation, admitted only on significant held-out lift (approved 82) | verified — BUILD-SPECS |
| B2 | BUILD-SPEC: prerequisite-gap diagnosis via backward graph walk, confirm before redirect (approved 83) | verified — BUILD-SPECS |
| B3 | BUILD-SPEC: cross-agent fleet transfer — zero-trust, quarantined behind the §8 commit gate (approved 82) | verified — BUILD-SPECS |
| B4 | BUILD-SPEC: forgetting-aware spacing — `R(t)=exp(−t/S)`, review-budget cap (approved 82) | verified — BUILD-SPECS |
| C1 | The human verifier line (human-learning instantiation). **Tag-only in v2**; the Frappe/ERPNext instantiation is deferred; its docs stay as research reference | verified — RELEASE-PLAN §4, HANDOVER-v2 §6 |
| SOLVE/JUDGE | §17 partition: `self_modify` may edit only SOLVE (the solve-scaffold); JUDGE (verifier, held-out + generator, gates, posterior, orchestrator) has no agent write-path, ever | verified — spec §17.1 |
| §19 / self-calibrating gate | Gate thresholds tuned by measured learning, clamped so the effective gate is never looser than §8; approved 85/100 on 2026-06-29, spec edit currently uncommitted | verified — S19 decision record |
| review-360 gate | The two-stage governance flow for every spec change: `review-360` (multi-viewpoint 0–100 review, critical-floor aggregation) → `change-approver` (APPROVE/REJECT: overall >80, correctness/red-team/safety ≥70, zero blockers, check-on-the-checker) | verified — .claude/agents/, reviews/ |
| M0 / M1 / M2 / M3 | The gated milestones: M0 prove real learning on held-out · M1 open schema + capability suite (after C0 re-red-team) · M2 weight axis (GPU) · M3 self-mod + fleet (§17/§18/§19) | verified — IMPLEMENTATION-v2 §1 |
| B7 | The M0 go/no-go acceptance test id (held-out beats frozen no-learning baseline beyond `z·SE`, probes fail as designed); the v1 "B7 GO" was baked, not real | verified — HANDOVER-v2 §1 |
| Phase A / B / C | The v2 build arc: A make M0 real (the whole game) · B implement the approved capability suite after C0 · C package & release | verified — HANDOVER-v2 §5 |
| Tier ① ② ③ | v2 maturity labels: ① working/validated core · ② experimental (spec-complete, unvalidated end-to-end) · ③ tag/design-only roadmap | verified — RELEASE-PLAN §4 |
| C0 | Mandatory re-red-team of the implementation before opening growth / starting Phase B (M1 precondition) | verified — HANDOVER-v2 §5 |
| 5-store | The data layer: SQL (truth) · document (inferred state) · vector · graph · cache; v2 default is the embedded tier (SQLite + networkx + local vector, zero infra) | verified — DATA-LAYER refs |
| EIG | Expected information gain — the exploration term in A1's blended objective | verified — BUILD-SPECS A1 |
| provision_suite | The eval-item generator for newly grown skills (growth-provisioning invariant, RC-3); part of JUDGE, never agent-writable | verified — spec §5.1/§17.1 |
| PAL | Personalized adaptive learning — the human-facing product layer; explicitly NOT in v2 scope | verified — RELEASE-PLAN §1 |
| memory axis / weight axis | The two improvement axes: memory/context-level (skills, lessons — free) vs weight-level (SFT/RL fine-tuning — M2, GPU) ; §17 adds a third: the scaffold (code) axis | verified — spec §17 note |
| [D-1] [D-2] [D-3] | Open owner decisions in RELEASE-PLAN-v2: versioning split · library housing · release cut line | verified — RELEASE-PLAN |
| M-R | The current milestone (HANDOVER-v3, 2026-07-28): **representative + resumable + unattended** — a GO or honest NO-GO on a real coding corpus, from a run no human supervised; supersedes HANDOVER-v2's phase framing | verified — HANDOVER-v3 §3 |
| G1–G7 | HANDOVER-v3's gap map from current state to autonomous MDLP: audit bugs · adopt approved spec deltas · wire-or-retract unwired constructs · **representative corpus (the credibility item)** · autonomy loop · model providers · observability | verified — HANDOVER-v3 §2 |
| R1 | The §16-companion retrieval BUILD-SPEC: 5-mode retrieval dispatch + learned fusion reranker (linear over 5 features); approved 87/100, 2026-07-13 | verified — BUILD-SPECS, reviews/ |
| §17.6 / scaffold-version log | Lineage schema for scaffold self-modification: FIX/DERIVE/CAPTURE operators, ancestry-checked CAPTURE; approved 84/100; M3-horizon (schema may land, no runtime until M3) | verified — spec §17.6, reviews/ |
| B2 Amendment A | Typed `part_of` graph edges, soft `τ_traverse` floor with `q_edge` inverse, and the `queue_rank` confirmation queue for prereq-gap redirects; approved 85/100 | verified — BUILD-SPECS, reviews/ |
| work unit / `intent_key` | DATA-LAYER §6.1 write discipline: every eval run is a leased work unit minted from a dispatch row; `open_work_unit` is idempotent on a UNIQUE `intent_key` (= dispatch event id) — kill -9 resume with no double-count and no lost evidence | verified — DATA-LAYER §6.1 |
| RedactedTruthView | §6.2 P1 boundary: the SOLVE bundle wraps the truth store in a view with held-out `item_ids`/content stripped and write-methods raising — measurement stays unreachable from optimization | verified — DATA-LAYER §6.2 |
| queue_rank | B2 Amendment A's ranked confirmation queue for candidate prerequisite gaps (confirm before redirect, ordered) | verified — BUILD-SPECS B2-A |
| AUT-1..4 | Automaton-study proposals: AUT-1 landed as spec §20 (87/100) and AUT-2 as DATA-LAYER §11 (83/100), both gate-approved 2026-07-30; AUT-3 (§17.5/§17.6 hardening) and AUT-4 (claims-TTL audit) remain queued ungated | verified — reviews/, STUDY-automaton-autonomy §6 |
| two-level loop | The autonomy skeleton (automaton adopt-#1): infinite outer supervisor (every exit = timed sleep) wrapping the existing bounded n-tick learning run as the inner cycle | verified — STUDY-automaton-autonomy, HANDOVER-v3 Phase 3 |
| potion / Model2Vec | Static-embedding family (potion-base-32M, MIT, numpy-only — no torch, no inference): MDLP's zero-infra `embed-lite` pick at ~93% of MiniLM quality; default `embed` = all-MiniLM-L6-v2; upgrade = Qwen3-Embedding-0.6B | verified — STUDY-small-models §2 |
| OpenSpace | HKUDS OpenSpace — closest shipped relative (self-evolving skill layer, experience counters, LLM-gated evolution); a point-estimate, statistics-free degenerate of MDLP and the natural paper baseline | verified — STUDY-raganything-agentscope-openspace |
| ObservabilityPort / AnalyticsStore | Opt-in autonomy-profile roles (DATA-LAYER §11, gate-approved 2026-07-30): embedded defaults = truth events/JSONL + SQLite views (zero-infra); full tier = Langfuse (traces) + ClickHouse (rebuildable analytics projection). Never default; all observability JUDGE-authored | verified — DATA-LAYER §11, DL-observability-roles-decision |
| S1–S9 | The 2026-08 arc's discovered spec-side gaps (vs. HANDOVER-v3's TA-side G1–G7): S1 no named safety properties · S2 reactive/incomplete structural constraints · S3 the spec ratchets (no compression term) · S4 no learning liveness · S5 granularity unprincipled · S6 growth bounded by the learner's own history · S7 stale-evidence handling ×5 vocabularies · S8 skills unportable · S9 paper-positioning gaps | verified — PLAN-harvest-2026-08 §1 |
| WP-A..WP-F | The six work packages collapsing the arc's 24 proposals: A author better (RAF-7/2 + compression) · B the invariant layer · C the corpus experiment (bridges G4) · D learning liveness · E growth beyond history · F positioning & portability. Sequenced A → B → {C, E}, D anytime, F continuous | verified — PLAN-harvest-2026-08 §2–§3 |
| invariant layer | WP-B's one architecture in five layers: declare invariants (RAF-1a property figure) → derive systematically (ONT-3 OntoClean) → enforce deterministically at write time (ONT-1′ constraint table at `merge()`) → maintain per stage (LIV-3) → check the rest by trace replay (RAF-1b), with epochs (RAF-3) as one property. Makes the IX-040 built-but-unwired defect class runtime-detectable | verified — PLAN-harvest-2026-08 WP-B |
| RAF-1..10 | Raft/Ousterhout-study proposals: 1a/1b safety-property figure + TruthStore-replay checker · 2 consolidate 4 duplicated mechanisms · 3 epoch discipline (retires `κ_reval`) · 4 implementability study · 5 fleet membership · 6 log/volume bound · 7 explain-it-back gate devices (landed framework-side) · 8 decomposition audit · 9 deterministic core · 10 name the control-plane story | verified — STUDY-ontologies-and-raft |
| ONT-1′, ONT-3..7 | Ontology-study proposals: 1′ generalize the approved acyclicity check into a declared constraint table (mechanism already built — the gap is the inventory) · 3 OntoClean meta-property tagging · 4 competency questions (defines "representative" for G4) · 5 stable skill ids + ESCO/CTDL mappings-not-merges · 6 name/benchmark the causal prereq story · 7 granularity instrumentation. ONT-2 folded into ONT-4 | verified — STUDY-ontologies-for-mdlp §7 |
| LIV-1..3 | Learning-liveness proposals (one §20.6/§20.7 delta): 1 learning heartbeat — three signals (evidence/progress/structure), surfacing never a breaker · 2 fraction of cells pinned at `n_min` · 3 per-stage validation discipline (B2's insertion-time check as pattern) | verified — NOTE-learning-liveness §5 |
| JMP-1..3 | LLMs-can't-jump proposals: 1 three-state liveness distinguisher (stalled / converged / converged-with-absorbed-anomaly-pattern) · 2 invention engine — SOLVE-side counterfactual variant generator, P1-separated, outcomes seed `F` through the ordinary gate · 3 PAPER scope statement (Peirce naming; evidence-free restructuring out of scope) | verified — STUDY-llms-cant-jump |
| VIS-1..4 | Prospection proposals (the owner's jump mechanism): 1 `previsit` · 2 vision nodes + backward design · 3/4 later stages, deferred until 1/2 prove | verified — NOTE-prospection-pathways |
| previsit | `previsit(H)` — the exact forward twin of §15's `revisit(D)`: process a hypothetical future mastery state through the current model to extract candidate skills/edges/variants; §15.3's budget governance transfers verbatim | verified — NOTE-prospection-pathways §3.1 |
| vision node | A skill posited without evidence, graph status `hypothesis` (joining `live | pending_human | retired`), reverse-chained to the frontier via B2's backward walk; provisioned by `provision_suite` on approach — becomes real, parks, or the vision retires. MDLP's first top-down/backward-design mechanism (Wiggins & McTighe) | verified — NOTE-prospection-pathways §3.2 |
| branch index | `Θ[skill, difficulty \| agent, generation, branch]`: `branch=0` is reality, `branch≥1` are cheap disposable projection sheets with a type-level write barrier — simulated outcomes touch no posterior ever ("projection proposes, reality disposes") | verified — NOTE-prospection-pathways §3.3 |
| Vulcan move / absorbed anomaly | Zahavy's H1 mapped to MDLP: `coherent_with` absorption lets individually-absorbable but collectively-patterned failures never reach `F` — a third liveness state (converged-with-absorbed-anomaly-pattern), watched via per-cell absorbed-failure rate (JMP-1) | verified — STUDY-llms-cant-jump H1 |
| ZPD band | Peer-teaching selection rule from Raft-misfit analysis: the teacher must significantly exceed the student but within `δ_max` — an anti-argmax band (zone of proximal development), vs Raft's most-complete-log voting | verified — NOTE-peer-learning-roles |
| spec-as-corpus | WP-C's design: MDLP's own spec sections + the ~120 stubs as real code+pytest tasks — one run yielding a G4 bridge corpus, the RAF-4 implementability study, and RAF-8's per-section difficulty map | verified — PLAN-harvest-2026-08 WP-C |
| [D-4] | New owner decision (2026-08-13): spec compression (spec-of-record ~12 sections + appendix-of-record) before or after the v2 tag; recommendation *before* | verified — RELEASE-PLAN-v2 status update |
| HANDOVER-v4 / H4-n | The 2026-08-18 task-list handover to TA (supersedes v3): 17 tasks `H4-1..17` in tiers T0–T4, mapped onto TA's own WBS ids (`W*`, e.g. G3 ≙ W4.7, G4 ≙ W3.0/W3.7); T1+T2 already built as the patch series | verified — HANDOVER-v4.md |
| H4 patch series | `patches/h4-series/` — 9 `git am`-ready patches vs TA `d9e78ac`, built+test-verified in an isolated clone (48 new tests, both TA suites green); TA never touched — applies only by the founder's hand (L-012) | verified — series README |
| crossing brief | The document that travels WITH a manual L-012 crossing, addressed to the receiving workspace's agent/operator: maps arriving artifacts onto that repo's own WBS/board/lessons, lists behavioral changes, gives rebase guidance, and keeps a two-way crossing ledger. First instance: `2026-08-19-mdlp-h4-crossing-brief.md` | verified — patches/h4-series/ |
| wire-or-retract | G3's rule for built-but-unwired constructs: every hermetic construct must either be wired onto a live path or formally retracted/parked with reasons + a guard test — never left silently dormant (TA's L-044 names the pathology; L-023: the wiring itself is a test that surfaces latent defects) | verified — HANDOVER-v3/v4, lessons |
| Zeno-slope problem | H4-10 finding: an asymptotic posterior yields a vanishing-but-forever-significant learning-progress slope, making Converged undetectable under pure significance testing — fixed by requiring `significant()`'s own margin (`eps_lp`); §20.10 should specify it | verified — h4-series README patch 0008 |
| disarmed-but-plumbed | A shipping state for a risky trigger: built + wired + tested + intentionally OFF at the call site, with the demonstrated failure modes documented in-code (H4-3's miscalibration rollback trigger). One instance so far — watched at backlog B-007, not yet a lesson | verified — h4-series README, RR-2026-08-19 |
| ROX-1 | Rox-study gated candidate: make §4.2's trajectory capture an explicit precondition (the H4 shape_ok retraction + Rox's auto-logged-versioned module-I/O pattern); rides the H4-findings gate batch | verified — STUDY-rox-agent-swarm §4.1 |
| admission against interest | L-015 grading refinement from the Rox study: within vendor-doc-class sources, a vendor's concession of what its own system LACKS outweighs its capability claims — the strongest evidence a vendor doc can carry (e.g. Rox's "LLM systems are stagnant") | verified — STUDY-rox-agent-swarm source-class header |
