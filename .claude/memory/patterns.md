# Patterns

Recurring code/process shapes worth standardizing. Each entry: a name, the canonical form, and the lessons it relates to. Maintained by `retrospective`. Stable `P-NNN` ids so other docs can link to them.

This file is **empty of project patterns on purpose** — patterns are extracted from real, repeated work, not seeded speculatively (L-003). The first `P-001` will appear when `retrospective` sees the same idiom land ≥3 times.

---

<!-- Example of the shape a pattern takes (delete once real patterns exist):

## P-001 — <name>

Used by: <where this shows up>.
Related lessons: L-0NN.

```<lang>
<the canonical implementation>
```

Why this and not the obvious alternative: <one line>.
-->
