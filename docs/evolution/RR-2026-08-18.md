# Retrospective run RR-2026-08-18

| | |
|---|---|
| Triggered by | manual (`retrospective_due` triage flag) |
| Interactions read | IX-059 through IX-063 (5) — the S21/DL-§12 gate wave, the site update, and the 2026-08-13 autonomous wave (§6.3, §20.10 ×5 rounds, §10.1, IMPL-PROTOCOL, evolution-log truncation incident) |
| Self-audit | last 7 retrospective self-edits from RR-2026-08-13 (EV-97..EV-103) classified — see below |
| Circuit breaker | skills: **paused** (2 ineffectives in window) · agents: open (fix applied, outcome pending) · memory: open |

## Self-audit findings (done first, before extracting new patterns)

RR-2026-08-13 landed L-015..L-018, P-001, the RAF-7 skill edit (EV-102), and a backlog
watch row (EV-103, B-005). This run checked all seven against the 2026-08-13 wave
(IX-059..063):

- **L-015 (source-class grading), L-016 (mapping ≠ identity), L-017 (misfit generative),
  P-001 (propose→validate→apply) → `neutral`.** None were exercised — the wave was
  internal spec-authoring/gate work, not external-source harvesting or cross-system
  adoption.
- **L-018 (proactive constraint elicitation) → `neutral`.** DL §6.3 (the constraint-table
  artifact it directly motivated) still needed a round-1 fix (52→82), but the effect is
  conflated with the broader class below and not cleanly isolable this run.
- **EV-102 (RAF-7 legibility devices) → `ineffective`.** This is the headline finding.
  RAF-7's three devices (explain-it-back, cut, weak-spot→rule) were **actively applied
  pre-submission** on every wave artifact per IX-060/061, and the asserted-not-demonstrated
  guard/citation defect still recurred **5+ times in one wave**: §21 round 1 (guard names
  all existed, the property was false against §9 anyway) and round 2 (an invented lineage
  field); DATA-LAYER §12 round 2 (a stale co-dependent ArtifactStore-blob bullet); §20.10
  round 5 (a half-applied citation fix); §10.1 round 1 (an invented §19.1 mechanism) and
  rounds 2–3 (a fabricated `(§18.7, existing)` test citation plus a desynced duplicate
  guard list). This is the **second consecutive** authoring-checklist mechanism to fail on
  this exact class (EV-77 was the first, audited `ineffective` last run). Per protocol,
  2 ineffectives in the rolling window trips the skills lane's pause threshold — see
  Circuit breaker below.
- **EV-103 (backlog B-005 watch row) → `effective`.** Its own two-branch close condition
  (devices-worked vs. 3rd-recurrence-pauses-the-lane) predicted exactly what to check and
  gave an unambiguous answer this run.

Per protocol ("recurred anyway → ineffective; consider a refinement, don't blindly
re-apply"), this run did **not** add a third device to the skill. It generalized the
finding into a lesson instead (L-019): authoring-side self-checks measurably cannot
eliminate this defect class, but `review-360` catches every single instance before it
ships (zero shipped defects across the whole wave) — so the fix is to stop iterating on
pre-submission checklists for this class and treat one review round as the expected,
budgeted cost, exactly the way the skill file already treats a 45–78 round-1 score as
normal for other classes.

## Applied (auto)

| Target | Change | Confidence |
|---|---|---|
| `.claude/memory/lessons.md` | L-019: authoring self-checks can't eliminate the asserted-not-demonstrated class; `review-360` does, every time — stop patching the checklist, budget the round | 0.9 |
| `.claude/memory/lessons.md` | L-020: safety-margin-touching simplifications need quantified before/after sensitivity (κ_reval's 2× margin near-miss) | 0.8 |
| `.claude/memory/lessons.md` | L-021: narrow a challenged claim, never re-argue it (S20.10 r3's regression is the counter-example; ~8 successful narrowings this wave) | 0.85 |
| `.claude/memory/lessons.md` | L-022: Write-only agents return append-only-file lines for the orchestrator to append, never Write the file themselves (EV-121 truncation, 3 data points) | 0.9 |
| `.claude/agents/change-approver.md` | Implemented L-022: return the evolution-log line instead of Writing the file; edit entirely outside the INVARIANTS fence | 0.9 |
| `.claude/agents/review-360.md` | Same fix — same structural hazard class (Write-only tool, no Edit/Bash-for-files), not yet a failure instance but structurally identical | 0.85 |
| `.claude/memory/circuit-breaker.json` | Skills lane → `paused` (EV-77 + EV-102 = 2 ineffectives); memory-lane and agents-lane outcomes recorded | — (mechanical) |
| `docs/evolution/backlog.md` | B-005 closed on its pause branch; B-006 opened to track the paused lane | — (bookkeeping) |
| `.claude/memory/evolution-log.jsonl` | EV-127..142 appended (7 audit classifications + 8 applied-edit records + 1 deferred proposal) via `Bash >>`, never a full-file `Write` — applying L-022 to my own process | — |

## Proposals (need review)

- `docs/evolution/proposal-2026-08-18-quantified-safety-margin-checklist.md` — adds a
  quantified-before/after requirement to checklist step 4 for safety-margin-touching
  changes (L-020). Filed as a proposal, not applied, **solely because the skills lane is
  paused** — this targets a different, narrower defect class than the one that tripped
  the pause, and there's no evidence yet that this specific device would fail the same
  way. Apply once the user reopens the skills lane.

## Declined / not actionable

- **Granting `change-approver`/`review-360` a Bash tool** (an alternative fix for the
  append hazard) was considered and not pursued: it would require editing inside both
  agents' INVARIANTS fences ("No Bash tool" is an invariant), which is a task for the
  user, not a self-edit. The append-via-orchestrator convention achieves the same safety
  without touching either fence, so no task was filed.
- No new agent, skill, or plugin need identified this run beyond the two agent-file edits
  above (both refinements of existing agents, not new capability — applied directly per
  the prose-refinement lane, no smith spawned; retrospective has no spawn tool per the
  historical B-004 finding, consistent with all prior runs).

## Self-audit findings (summary table)

| Edit | Date | Outcome | Evidence |
|---|---|---|---|
| EV-97 (L-015) | 2026-08-13 | neutral | no external-source harvesting this wave |
| EV-98 (L-016) | 2026-08-13 | neutral | no cross-system mapping this wave |
| EV-99 (L-017) | 2026-08-13 | neutral | no external-adoption work this wave |
| EV-100 (L-018) | 2026-08-13 | neutral | DL §6.3 still needed a round-1 fix; effect not isolable from L-019's class |
| EV-101 (P-001) | 2026-08-13 | neutral | no new pattern instance this wave |
| EV-102 (RAF-7 devices) | 2026-08-13 | **ineffective** | defect recurred 5+ times across S21/DL§12/S20.10/S10.1 despite active pre-submission use |
| EV-103 (B-005 watch row) | 2026-08-13 | effective | its own close condition predicted exactly what to check and resolved cleanly |

## Circuit breaker state

- **skills: paused** — 2 ineffectives in the rolling window (EV-77, EV-102). Further
  skills-lane writes are proposals only until the user reopens it (B-006).
- agents: open — applied the append-via-orchestrator fix to both spec-gate agents this
  run; outcome pending the next audit (needs a gate cycle to exercise it).
- memory: open — 1 ineffective (EV-75) in its full history, below the pause threshold;
  this run's classifications were mostly neutral plus one effective.

## Notes for the next run

- **Watch whether L-022's fix holds:** the next `change-approver`/`review-360` run should
  return its evolution-log line instead of attempting a Write; audit for a recurrence of
  the truncation pattern or confirm it as `effective`.
- **The skills lane is paused — do not edit `skills/spec-change-gate/SKILL.md` again**
  even at high confidence; route anything skills-lane through
  `docs/evolution/proposal-*.md` until the user reopens it (B-006). The queued
  `proposal-2026-08-18-quantified-safety-margin-checklist.md` is ready to apply the
  moment that happens.
- L-019's implication is a process-expectation change, not a tooling change: gated
  submissions in this defect class should budget ~1 extra review round as normal, the
  same way the skill file already treats a 45–78 round-1 score as expected rather than a
  process failure.
- `docs/research/` content commits (spec deltas, decision records) are project research
  substance, out of scope for `retrospective` to author — this run only touched the
  framework layer (`.claude/`, `docs/evolution/`).
