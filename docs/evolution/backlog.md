# Evolution backlog

The framework's own task queue. Lifecycle: `queued → in-progress → done | dropped`. Append-and-amend; never delete a row.

| ID | Item | Type | Status | Evidence | Opened | Close condition |
|---|---|---|---|---|---|---|
| B-001 | Profile the project (run `scout`) | profile | done | project UNPROFILED | unset | project-profile.md no longer says UNPROFILED — met: deep profile landed 2026-07-02, commit 3236339 |
| B-002 | Fix the unattended curator daemon (never completed a successful run: chmod +x missing on 2026-06-22 install → `Operation not permitted`; then 401 auth failures 2026-06-25/26) | infra | queued | `.claude/daemon/launchd.err.log`, `.claude/daemon/runs/20260625-212941.log`, `.claude/daemon/runs/20260626-173811.log` | 2026-07-02 | a `.claude/daemon/runs/*.log` entry shows a completed run (no 401/permission error) after re-running `scripts/install.sh --daemon`, or the user explicitly drops unattended mode in favor of in-session-only evolution. **2026-07-28 staleness check:** `.claude/daemon/runs/` is unchanged since 2026-06-26 (still just the two failed 401 logs) — no new attempt in 4 audit passes (2026-07-02, -03, -28) spanning 8+ real interactions; all evolution to date (this run included) has been in-session/manual, confirming L-011's standing advice. Surfacing as genuinely stale — needs either a real retry or a user decision to drop unattended mode |
| B-003 | Build `skills/spec-change-gate/` (review-360 → change-approver playbook: round-numbering convention, decision-record format, "approver authorizes, never commits") | skill | done | scout-proposals-2026-07-02.md; flow ran 9+ times by 2026-07-03 (A1, A5, B1–B4, §16, §17/§18, §19), then 4 more full multi-round cycles 2026-07-13 (R1, S17.6, DL-write-discipline, B2-amendA — ~30 rounds, all reaching APPROVED) with zero round-numbering or decision-record-format deviations; full brief at docs/evolution/proposal-2026-07-02-spec-change-gate-skill.md | 2026-07-02 | **Met 2026-07-28.** `skills/spec-change-gate/SKILL.md` exists (since 2026-07-03, EV-45) and the exact procedure it documents (round-numbering, decision-record naming, reviewer/approver separation, "approver authorizes, never commits") was followed correctly across all 4 new 2026-07-13 gate cycles — the operational content is now embedded practice. Caveat: the reviews themselves don't cite the skill file by name (0 hits for "spec-change-gate" in `docs/research/reviews/`), so this is closed on adherence-to-content, not literal citation; if a future gate run deviates from the convention, reopen this row rather than opening a new one |
| B-004 | Retrospective has no `Task`/`Agent` spawn tool in-session, so it cannot execute the delegate-building rule (spawn `skill-smith`/`agent-smith`/`plugin-researcher`) — it can only file proposals, even at 0.9 confidence | infra | queued | RR-2026-07-02 (EV-42), RR-2026-07-03, and this run (2026-07-28) all hit the identical blocker — 3/3 retrospective runs so far | 2026-07-03 | a retrospective run successfully spawns a smith agent directly (not via proposal), or the user confirms proposals are the intended permanent handoff mechanism and this row is dropped. **2026-07-03 resolution note:** the immediate B-003 blocker was resolved not by retrospective gaining a spawn tool, but by the user directly instructing main-Claude to execute the skill-smith brief in-session (delegation, not spawn) — `skills/spec-change-gate/SKILL.md` now exists via this path. **2026-07-28 update:** this run's candidate patterns needed only lesson/prose edits (retrospective's own direct-edit lane), so the spawn-tool gap wasn't a blocker this time — but the underlying tooling gap is unchanged and confirmed a 3rd time; still queued |
